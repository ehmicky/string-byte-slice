/**
 * `string-byte-slice` options
 */
export interface Options {}

/**
 *
 * @example
 * ```js
 * ```
 */
export default function stringByteSlice(
  input: string,
  byteStart: number,
  byteEnd: number,
): string
